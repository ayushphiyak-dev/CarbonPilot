<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120">
  <defs>
    <linearGradient id="ringGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#9EEB47"/>
      <stop offset="100%" stop-color="#3C8100"/>
    </linearGradient>
  </defs>
  <!-- Outer gradient ring -->
  <circle cx="60" cy="60" r="60" fill="url(#ringGrad)" />
  <!-- Inner transparent cutout (white) -->
  <circle cx="60" cy="60" r="30" fill="#FFFFFF" />
  
  <!-- Compass Arrow Base (darker green) -->
  <path d="M 36 48 L 84 44 L 56 68 Z" fill="#4B8C00" />
  <!-- Compass Arrow Right/Bottom (lighter green) -->
  <path d="M 36 48 L 56 68 L 48 88 Z" fill="#78C627" />
</svg>
